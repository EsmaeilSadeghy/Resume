ط/**
 * A simple greeting function.
 * @param {string} name - The name of the person to greet.
 * @returns {string} Greeting message.
 */
function greet(name) {
    return `Hello, ${name}! Welcome to Esmaeil Sadeghi's module.`;
}

module.exports = greet;